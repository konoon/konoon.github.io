package source17_lambda.exam04;

// 클래스의 멤버 사용
// 람다식 실행 블록에는 클래스의 멤버의 필드와 메서드 제약 없이 사용함
// 람다식 실행 블록 내에서 this는 람다식을 실행한 객체의 참조

public interface MyFuntionalInterface {
	public void method();
}
